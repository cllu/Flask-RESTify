============
Installation
============

At the command line::

    $ easy_install Flask-RESTify

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Flask-RESTify
    $ pip install Flask-RESTify
